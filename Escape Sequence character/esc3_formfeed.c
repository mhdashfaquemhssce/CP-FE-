/* 3. Form Feed Escape Sequence In C (\f)
The form feed character (ASCII code 12) is a control character
that historically caused the printer to advance to the next page or form.
In modern systems, it often represents a vertical spacing or page break in the output. */
#include <stdio.h>

int main() {
printf("I am \f Unstoppable");

return 0;
}
