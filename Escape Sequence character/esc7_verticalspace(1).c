/* 7. Vertical Tab Escape Sequence In C (\v)
The vertical tab character (\v), as the name suggests,
adds a vertical space between the two sections of the string, i.e.,
the sections before and after the escape sequence. */

#include <stdio.h>

int main()
{
    printf("I am \v Unstoppable");

    return 0;
}
